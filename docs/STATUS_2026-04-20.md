# LV-Preisrechner — Status 20. April 2026

## Was heute passiert ist

### 1. Legacy-Aufräumen
- `/backend` und `/frontend` nach `/_archive/cockpit-legacy/` verschoben (mit `git mv`, History erhalten)
- Weitere Legacy-Artefakte archiviert: `/docker-compose.yml`, `/render.yaml`, `/tests`, `/shared`, `/prompts`
- Root-Verzeichnis sauber strukturiert: nur noch `lv-preisrechner/`, `knowledge/`, `docs/`, `_archive/`
- README aktualisiert: Fokus explizit auf `lv-preisrechner/`, Stack-Info korrigiert (PostgreSQL statt Prisma, JWT statt Clerk, Render statt Railway)

### 2. Parser-Qualität massiv verbessert
- Knauf-System-Taxonomie erweitert: **W628, W628A, W630, W631, W133** präzisiert/neu
- Bisher fehlende Kategorien im `SYSTEM_PROMPT` ergänzt: Streckmetalldecke, Deckensegel, Wandabsorber, Deckenschott, Wandanschluss, Deckensprung, Verstärkungsprofil, Kabeldurchführung F90, Aufdopplung_geklebt
- 4 neue Rezepte für vorher unerkannte Kategorien im Stuttgart-LV: **GK_Schwert, Leibungsbekleidung, Freies_Wandende, Stützenbekleidung**
- Falschklassifizierung im `Verkleidung`-Bucket **von 23 auf 1 reduziert** (-96 %)

### 3. Zwei echte Bugs gefixt
- Bedarfspositionen wurden in Angebotssumme einbezogen → **gefixt**
- Alternativpositionen wurden in Angebotssumme einbezogen → **gefixt**
- DB-Migration `8f2c1a9b4d01_add_is_bedarf_is_alternative` erstellt (fügt `is_bedarf` + `is_alternative` an `lvp_positions` + drei neue Summen-Spalten an `lvp_lvs`)
- Neue Summen-Felder auf `LV`:
  - `angebotssumme_netto` (bindend, ohne optional)
  - `bedarfspositionen_summe` (informativ)
  - `alternativpositionen_summe` (informativ)
  - `gesamtsumme_inklusive_optional` (zur Referenz)

### 4. Qualitätskontrolle eingeführt
- Golden-Test mit echtem Stuttgart-LV (Peter Gross Bau, 48 Seiten, ~100 Positionen)
- Snapshots committet: `stuttgart_raw_parse.json` (v1), `..._v2.json`, `..._v3.json`
- 9 Assertions decken kritische Parser-Fälle ab
- Test läuft bei Regression automatisch rot
- Drift-Warnung bei Abweichung > 10 Positionen vom Snapshot-Referenzwert

## Aktueller Test-Stand
- **59 Tests grün** (43 bestehende + 7 neue Unit-Tests + 9 Golden)
- Keine roten Tests
- Golden-Test als `@pytest.mark.integration` markiert (läuft nicht bei jedem Commit, nur manuell oder nightly — ausschließen via `pytest -m "not integration"`)
- Marker in `pyproject.toml` registriert

## Was beim nächsten Arbeitstag wichtig ist

### Sofort zu tun (Deployment-Voraussetzung)
- **Migration ausführen:** `alembic upgrade head`
  - Fügt `is_bedarf`, `is_alternative` auf `lvp_positions` hinzu
  - Fügt `bedarfspositionen_summe`, `alternativpositionen_summe`, `gesamtsumme_inklusive_optional` auf `lvp_lvs` hinzu
- Existierende Rows bekommen Defaults (`false` bzw. `0`) via `server_default`
- Kein Datenverlust, kein manuelles Daten-Backfilling nötig

### Offene TODOs (dokumentiert im Code als Kommentar-Markierung)
- **Mengenansätze der 4 neuen Rezepte gegen DIN 18181 verifizieren:** GK-Schwert, Leibungsbekleidung, Freies Wandende, Stützenbekleidung — derzeit geschätzt, mit `# TODO` markiert in `materialrezepte.py`
- **Knauf-Systeme W628, W628A, W630 gegen offizielle Datenblätter verifizieren** — derzeit aus Modellwissen + Stuttgart-LV-Text abgeleitet, keine Web-Recherche durchgeführt
- **Knauf-Wissensdatenbank** in `knowledge/knauf-systeme-*.json` bleibt mit Hinweis *"aus Modellwissen rekonstruiert, Validierung gegen offizielle Knauf-Detailblätter ausstehend"*

### Empfohlene nächste Aufgaben (mit Begründung)
1. **Kemmler-Preisliste als strukturierte Daten einbauen**
   → Ersetzt modellgenerierte Knauf-Preise durch reale Händlerpreise, macht Kalkulation belastbar.

2. **Multi-Tenant-Preisarchitektur**
   → Jeder Kunde kann eigene Einkaufspreise hinterlegen und Standard-Preisliste überschreiben — Voraussetzung für SaaS-Betrieb mit mehreren Pilot-Kunden.

3. **Zweites Stress-LV von anderem Gewerk**
   → Parser-Robustheit über Trockenbau hinaus testen. Bis jetzt: Habau (Koblenz) + Gross (Stuttgart) — beides klassische Trockenbau-LVs. Für Malerarbeiten, Fliesen, Estrich braucht es eigene Rezept-Familien.

4. **Ground-Truth-Abgleich mit echten Angeboten**
   → Harun's Vater fragen, ob er fertige Angebote zu den beiden vorliegenden LVs (Habau + Gross) hat. Erst damit wird **Preis-Korrektheit** messbar — alle bisherigen EUR-Schätzungen im Projekt basieren auf Branchen-Durchschnitten.

## Bekannte Beschränkungen
- **Parser-Laufzeit:** 7 Minuten pro 48-seitiges LV (Claude Vision, Sonnet 4.6, Batch-Größe 5). Zu lang für Live-UX, akzeptabel für Batch/Async-Verarbeitung.
- **EUR-Kalkulation im Snapshot** basiert auf Durchschnitts-EPs je Einheit (50 €/m², 150 €/Stk usw.), nicht auf echter Preisliste + Rezept-Kalkulation. Erst nach Integration der Preisliste lieferbar.
- **Bedarfs-Bug-Fix nur mit 0,07 % Summen-Diff validiert** (Stuttgart-LV: alle 6 Bedarfspositionen sind Stück-Positionen mit Menge = 1). Bei LVs mit großen Bedarfsmengen (z. B. pro m² als Bedarfsposition) kann der Impact dramatischer sein — aber strukturell ist der Fix konsistent.
- **Parser-Varianz** bei Claude Sonnet: Positions-Anzahl schwankt ±5-10 zwischen Läufen (93/102/103 in drei Messungen). Deshalb Untergrenze 90 statt 95 in Golden-Assertion 1, plus Drift-Warnung.
