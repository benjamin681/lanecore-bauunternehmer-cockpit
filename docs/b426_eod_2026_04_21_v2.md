# End-of-Day-Status — 21.04.2026, 22:00

**Branch:** `claude/beautiful-mendel`
**Remote-HEAD:** `5d41ef0` (docs: UI wording guide + Mockup v1)
**Lokaler HEAD:** `9dc63b3` (docs: Option C Phase 1 baseline correction)
**Differenz:** **13 Commits lokal vor Remote — nicht gepusht**
**Tests:** 383 / 383 grün
**Working Tree:** clean (abgesehen von `.claude/launch.json` und
`lv-preisrechner/tests/live/test_api_smoke.py` — beide lokale IDE-/
Smoke-Tool-Artefakte, kein Repo-Relevanz)

## Commit-Stack (lokal, ungepusht)

| # | Hash | Block | Titel |
|---|---|---|---|
| 13 | `9dc63b3` | B+4.2.6 Opt. C | docs: Option C Phase 1 baseline correction |
| 12 | `b9fbd5b` | B+4.2.6 Opt. C | test: Phase 1c — 21 unit tests for product code extraction |
| 11 | `011001a` | B+4.2.6 Opt. C | feat: Phase 1b — product_code_extractor module + parser integration |
| 10 | `ad836ab` | B+4.2.6 Opt. C | docs: Phase 1 baseline — tokenization schema |
| 9 | `9c6c5f8` | B+4.2.7 | feat(worker): Teil 2 complete |
| 8 | `8614638` | B+4.2.7 | feat(resolver+parser): integrate pieces-per-length fallback |
| 7 | `a691a92` | B+4.2.7 | feat(resolver): resolve_pieces_per_length handles €/m |
| 6 | `e49a347` | B+4.2.7 | test: golden tests for pieces-per-length |
| 5 | `dce8c25` | B+4.2.7 | docs: baseline before bundle resolver extension |
| 4 | `3ee1175` | B+4.2.6 | docs: end-of-day status — matcher fix incomplete |
| 3 | `879b9f6` | B+4.2.6 | fix(matcher): separate dimension tokens |
| 2 | `f3c5d10` | B+4.2.6 | test: golden + guard tests for CW/UW matcher |
| 1 | `b490081` | B+4.2.6 | docs: baseline before matcher fix |

## Was heute erreicht wurde

### B+4.2.6 Option C Phase 1 — **abgeschlossen**

- Deterministischer Python-Post-Processor `extract_product_code(name)`
  (`app/services/product_code_extractor.py`, 64 LOC).
- Integration in `pricelist_parser._build_entry`: neue Keys
  `product_code_type` / `product_code_dimension` / `product_code_raw`
  werden in `attributes` gesetzt, wenn ein Code erkannt wird.
- 21 Unit-Tests (Happy-Path + Guards + Integration) — alle grün.
- Baseline-Doc + Korrektur-Abschnitt dokumentieren die
  Design-Entscheidungen (strenge Regex, erstes Vorkommen gewinnt,
  CD60/27 → CD60 Trade-off, CW-Profil ohne Code per Design).

### B+4.2.7 — **abgeschlossen** (bleibt lokal ungepusht bis B+4.2.6 erledigt)

- `resolve_pieces_per_length(entry)` entpackt €/m-Bundle-Preise
  (CW-Profil 167,40 → 8,05 €/m).
- Worker-Integration: `backfill_effective_units` wird nach jedem Parse
  aufgerufen.
- Backfill auf bestehende Kemmler-Liste: 80 Entries korrigiert (11
  Pfad B neu, 69 Pfad A technische Schuld).
- E2E auf Stuttgart-LV: Material netto **536 k € → 204 k €** (−62 %
  gegenüber dem B+4.2.6-only-Nachmittags-Stand).

### B+4.2.6 — **Status: Phase 1 Option C fertig, Phase 2 morgen**

- Matcher-Fix vom Vormittag (`879b9f6`) liegt weiter lokal, mit
  bekannter PE-Folie-UT40-Regression.
- Option-A/B/C-Entscheidung zugunsten **Option C** gefallen:
  Parser-seitige strukturierte Token-Extraktion, Matcher liest
  `attributes.product_code_type` mit Whitelist.
- Phase 1 von Option C heute komplett gebaut; der Matcher ist noch
  nicht umgestellt.

## Was morgen als erstes zu tun ist

**B+4.2.6 Option C Phase 2: Matcher-Umbau**

1. `price_lookup.py` / `material_normalizer.py` erweitern: wenn
   `query.product_code_type` ∈ Whitelist und
   `candidate.attributes.product_code_raw == query.raw`, dann harter
   Match. Ansonsten bestehender Fuzzy-Pfad.
2. Whitelist: `{CW, UW, UA, CD, UD, KKZ, SLP, MP}`. `UT`, `WLG`, `TP`
   sind explizit **nicht** drin (löst UT40-Regression, hält TP-Tür-
   pfosten draußen).
3. Backfill-Skript: `extract_product_code` rückwirkend auf die 327
   Kemmler-Entries anwenden (~10 Minuten, kein API-Call).
4. E2E auf Stuttgart-LV — erwartet:
   - Material netto zurück im Bereich **~130–160 k €**
   - Angebotssumme **~980 k €**
   - PE-Folie-UT40-Regression weg
   - CW-Profil-Match via Fuzzy-Fallback erhalten
5. Nach grünem E2E: alle 14 lokalen Commits pushen.

## Offene Punkte zum Nachdenken für morgen

- **Whitelist-Wartung:** neue Lieferanten bringen ggf. neue Codes mit
  (Hornbach Putzbänder hat z. B. andere Typ-Kürzel). Konzept für
  tenant-/supplier-spezifische Whitelist-Erweiterung?
- **Phase-2-Integration-Tests:** mindestens der UT40-False-Positive
  sollte als neuer Golden-Test abgesichert werden. Die 6 bestehenden
  CW/UW-Matcher-Golden-Tests aus B+4.2.6 bleiben.

## Regeln für den Start morgen

- Kein Push vor grünem E2E auf Stuttgart-LV.
- Keine weiteren Scope-Erweiterungen im Matcher (nur Whitelist-Pfad,
  Fuzzy-Fallback bleibt unverändert).
- Bei Regression in bestehenden Tests: STOPP, Analyse.
